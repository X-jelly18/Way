{
  "name": "nevermore-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Workers HTTP/WebSocket proxy for NevermoreSSH",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "latest"
  }
}
